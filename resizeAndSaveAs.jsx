#target photoshop// in case we double clicked the fileapp.bringToFront();var docRef = app.activeDocument;var myW = docRef.width;var myH = docRef.height;var outputPath =  docRef.path;if (myW > 2000 || myH > 2000) {        docRef.saveAs(new File(docRef.path.toString() + " FV sc7 ccs.tif"))					// do not modify the original	docRef.close(SaveOptions.DONOTSAVECHANGES)        } else {            docRef.saveAs(new File(docRef.path.toString() + " FV ccs.tif"))					// do not modify the original	docRef.close(SaveOptions.DONOTSAVECHANGES)}